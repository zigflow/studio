node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

*.schema.json
*.schema.yaml

# Generated by Paraglide (i18n). Regenerated by `npm run i18n:compile`, the vite
# plugin, and the `check` script. Listed here (as well as the dir's own emitted
# .gitignore) so ESLint's includeIgnoreFile picks it up too.
/src/lib/paraglide/
